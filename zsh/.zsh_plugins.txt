# Git integration and aliases
ohmyzsh/ohmyzsh path:plugins/git

# Auto-fetch git updates
ohmyzsh/ohmyzsh path:plugins/git-auto-fetch

# Terraform CLI support (aliases: tf tfi tfp tfa tfd tff tfv; workspace in prompt)
ohmyzsh/ohmyzsh path:plugins/terraform

# Docker aliases and completions (dps dpsa di dex dlog dprune; NOTE: omz defines
# `d` as an alias — we shadow it with our own d() function loaded after plugins)
ohmyzsh/ohmyzsh path:plugins/docker

# Docker Compose aliases (dc dcu dcd dcl)
ohmyzsh/ohmyzsh path:plugins/docker-compose

# Kubectl aliases and completions (k kgp kgs kgd kaf klo kdp + many more)
ohmyzsh/ohmyzsh path:plugins/kubectl

# AWS CLI tab-completion
ohmyzsh/ohmyzsh path:plugins/aws

# Command suggestions
zsh-users/zsh-autosuggestions

# Extra completions
zsh-users/zsh-completions

# Fuzzy tab completion
Aloxaf/fzf-tab

# Tmux integration
ohmyzsh/ohmyzsh path:plugins/tmux

# Syntax highlighting (must load second-to-last; history-substring-search depends on it)
zsh-users/zsh-syntax-highlighting

# History search with arrows (must load last)
zsh-users/zsh-history-substring-search
