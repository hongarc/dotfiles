# Window switching
bind u next-window        # prefix + u → next
bind y previous-window    # prefix + y → previous

# Choose-window navigation (Colemak)
# bind-key -T choose-window h tree-collapse
# bind-key -T choose-window n cursor-up
# bind-key -T choose-window e cursor-down
# bind-key -T choose-window i tree-expand

# Start windows and panes at index 1
set -g base-index 1
set -g pane-base-index 1